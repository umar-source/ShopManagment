@model List<SM.Business.Models.ProductModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@foreach (var product in Model)
{
    <div>
        <h4>ProductModel</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => product.Id)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => product.Id)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => product.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => product.Name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => product.Description)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => product.Description)
            </dd>
        </dl>
    </div>
}

<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
